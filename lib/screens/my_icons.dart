import 'package:flutter/material.dart';

class MyIcons {
  static const Icon user = Icon(Icons.person);
  static const Icon mail = Icon(Icons.mail);
  static const Icon lock = Icon(Icons.lock);
}

     const Color ayooPink = Color(0xFFFB11C8);